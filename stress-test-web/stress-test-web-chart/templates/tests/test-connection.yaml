apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stress-test-web-chart.fullname" . }}-test-connection"
  labels:
    {{- include "stress-test-web-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stress-test-web-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
